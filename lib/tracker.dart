import 'package:flutter/material.dart';

class Tracker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('data');
  }
}

// TODO use hero widgets for eating and craving tracker
